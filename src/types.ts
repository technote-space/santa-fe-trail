export enum FieldFlags {
  None    = 0, // eslint-disable-line no-magic-numbers
  Food    = 1 << 0, // eslint-disable-line no-magic-numbers
  Visited = 1 << 1, // eslint-disable-line no-magic-numbers
}
